# LangChain core
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.3.0

# MCP client for LangChain
langchain-mcp-adapters>=0.2.0

# Vector store — OCI PostgreSQL + pgvector
langchain-postgres>=0.0.12
psycopg[binary]>=3.1.0

# Text splitting
langchain-text-splitters>=0.3.0

# OCI SDK (Instance Principal auth + GenAI)
oci>=2.130.0

# Document loaders
pypdf>=5.0.0

# FastAPI backend (replaces Streamlit)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Async bridge (still needed by agent.py)
nest_asyncio>=1.6.0

# Env file support (local dev)
python-dotenv>=1.0.0

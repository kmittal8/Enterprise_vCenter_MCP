# ─────────────────────────────────────────────────────────────────────────────
# Enterprise vCenter MCP — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── vCenter connection ────────────────────────────────────────────────────────
# The OCI VM must have network connectivity to this host on port 443.
# Pre-establish via FastConnect, VPN, or OCVS VCN peering before deploying.
VCENTER_HOST=10.0.12.2
VCENTER_USERNAME=administrator@vsphere.local
VCENTER_PASSWORD=your-vcenter-password-here
VCENTER_PORT=443
VCENTER_SSL_VERIFY=false

# ── OCI GenAI ─────────────────────────────────────────────────────────────────
# Your OCI compartment OCID (required)
COMPARTMENT_ID=ocid1.compartment.oc1..aaaaaaaaxxx

# OCI GenAI region — ap-hyderabad-1 is the default
# Other options: ap-melbourne-1, us-chicago-1, eu-frankfurt-1
OCI_GENAI_REGION=ap-hyderabad-1

# Auth type:
#   instance_principal  — use on OCI Compute VM (no credentials needed)
#   api_key             — use for local development with ~/.oci/config
OCI_AUTH_TYPE=instance_principal

# ── OCI PostgreSQL (pgvector) ─────────────────────────────────────────────────
# Connection string to your OCI PostgreSQL DB system.
# Format: postgresql+psycopg://username:password@private-endpoint:5432/dbname
# The DB must have the pgvector extension enabled.
PG_CONNECTION_STRING=postgresql+psycopg://vcenteruser:yourpassword@10.0.1.100:5432/vcenterdb
PG_COLLECTION_NAME=vcenter_runbooks

# ── Optional overrides (defaults are fine for most deployments) ───────────────
# LLM_MODEL_ID=cohere.command-a-03-2025
# EMBED_MODEL_ID=cohere.embed-multilingual-image-v3.0
# LLM_TEMPERATURE=0
# LLM_MAX_TOKENS=2048
# RAG_TOP_K=4
# RAG_CHUNK_SIZE=800
# RAG_CHUNK_OVERLAP=100
